#include <stdio.h>
/**
 * main-Imprime frase :with proper grammar, but the outcome is a piece of art,
 * Return: Se deja en 0 puesto que la funcion esta vacia
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
    
}
