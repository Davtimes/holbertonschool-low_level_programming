#include <stdio.h>
/**
 * main - Escribe todo el alfabeto sin las letras e y q
 * Return: retorna cero 0
*/
int main(void)
{
	int az;

	for (az=0; az<=9; az++)
    printf("%d", az);
	putchar('\n');
	return (0);
}
